#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: the integer ro be checked
 * Return: Absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
